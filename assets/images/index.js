const imageUrl = "https://pub-606f905af66843f3aa385a802f9370c2.r2.dev/assets/images"

const logo = `${imageUrl}/logo.svg`;
const quadtrust = `${imageUrl}/quadtrust.png`;
const bchurunway = `${imageUrl}/bchu-runway.png`;
const jagota = `${imageUrl}/jagota.png`;
const propertyscout = `${imageUrl}/property-scout.svg`;
const udemy = `${imageUrl}/udemy.png`;
const chess = `${imageUrl}/chess.png`;
const chat = `${imageUrl}/chat.png`;
const onlineExam = `${imageUrl}/online-test.png`;
const traveler = `${imageUrl}/traveler.png`;
export {
    logo,
    quadtrust,
    bchurunway,
    jagota,
    propertyscout,
    udemy,
    chess,
    chat,
    onlineExam,
    traveler
}