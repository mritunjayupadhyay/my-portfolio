const iconUrl = "https://pub-606f905af66843f3aa385a802f9370c2.r2.dev/assets/icons";

const express = `${iconUrl}/express.svg`;
const css = `${iconUrl}/css.svg`;
const git = `${iconUrl}/git.svg`;
const github = `${iconUrl}/github.svg`;
const html = `${iconUrl}/html.svg`;
const javascript = `${iconUrl}/javascript.svg`;
const mongodb = `${iconUrl}/mongodb.svg`;
const motion = `${iconUrl}/motion.svg`;
const mui = `${iconUrl}/mui.svg`;
const nextjs = `${iconUrl}/nextjs.svg`;
const nodejs = `${iconUrl}/nodejs.svg`;
const react = `${iconUrl}/react.svg`;
const redux = `${iconUrl}/redux.svg`;
const sass = `${iconUrl}/sass.svg`;
const tailwindcss = `${iconUrl}/tailwindcss.svg`;
const typescript = `${iconUrl}/typescript.svg`;
const linkedin = `${iconUrl}/linkedin.svg`;
const summiz = `${iconUrl}/summiz.svg`;
const arrow = `${iconUrl}/arrow.svg`;
const contact = `${iconUrl}/contact.svg`;
const pointright = `${iconUrl}/pointright.png`;

export {
    css,
    express,
    git,
    github,
    html,
    javascript,
    mongodb,
    motion,
    mui,
    nextjs,
    nodejs,
    react,
    redux,
    sass,
    tailwindcss,
    typescript,
    linkedin,
    summiz,
    arrow,
    contact,
    pointright
}